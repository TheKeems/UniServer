{
  "name": "uniserver",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Accounts, profile storage and survey telemetry for Acceptiversity",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "dev:memory": "node dev-memory.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0"
  }
}
